extern crate clap;
use clap::{Arg, App};

fn main() {

    let matches = App::new("Kraken")
        .version("0.1.0")
        .author()
        .about()
        .arg()

    println!("Hello, world!");
}
